pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "qualtive-client-android"

include(":qualtive")
include(":demo")
enableFeaturePreview("TYPESAFE_PROJECT_ACCESSORS")
